export function generateStaticParams() {
  return [
    { action: "bridge" },
    { action: "airdrop" },
    { action: "redeem" },
  ];
}

export default function Page() {
  // see layout in the parent folder
  return null;
}
