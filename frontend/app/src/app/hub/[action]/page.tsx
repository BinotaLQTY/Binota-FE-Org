export function generateStaticParams() {
  return [
    { action: "bridge" },
    { action: "points" },
    { action: "airdrop" },
  ];
}

export default function Page() {
  // see layout in the parent folder
  return null;
}
